# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/todoDB
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Cohere API Key
cohere.api.key=RYQJewYMyXIelKmU5EPpEbHFs9bM0if9ka0RCOtJ

# Slack Webhook URL
#slack.webhook.url=${SLACK_WEBHOOK_URL}

# Server Port
server.port=9999
slack.webhook.url=https://hooks.slack.com/services/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

# ================================
# CORS CONFIG (so React frontend can talk to backend)
# ================================
spring.web.cors.allowed-origins=http://localhost:5173
